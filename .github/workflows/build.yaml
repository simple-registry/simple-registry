name: Build

on:
  push:
    branches: [ "main" ]
    tags: [ "v[0-9]+.[0-9]+.[0-9]+*" ]
  pull_request:

permissions:
  contents: write
  packages: write
  id-token: write

env:
  CARGO_TERM_COLOR: always
  GHCR_REPO: ghcr.io/${{ github.repository }}
  CONFORMANCE_IMAGE_NAME: conformance

jobs:
  rust-checks:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - name: Check formatting
      run: cargo fmt -- --check
    - name: Check clippy
      run: cargo clippy -- -D warnings
  rust-test:
    # We need to a redis backend for the tests
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
    - uses: actions/checkout@v7
    - name: Run tests
      run: |
        # Ensure redis service is running
        while ! nc -z localhost 6379; do
          echo "Waiting for redis to start..."
          sleep 1;
        done
        
        rustup component add llvm-tools
        cargo install grcov
              
        CARGO_INCREMENTAL=0 RUSTFLAGS='-Cinstrument-coverage' LLVM_PROFILE_FILE='cargo-test-%p-%m.profraw' cargo build
        
        docker run --rm -d \
          -e RUSTFS_ACCESS_KEY=root \
          -e RUSTFS_SECRET_KEY=roottoor \
          --network host \
          --name rustfs \
          rustfs/rustfs:1.0.0-beta.8

        # Ensure the S3 service is running, then create the "registry" bucket.
        while ! curl -sf -o /dev/null http://127.0.0.1:9000/health; do
          echo "Waiting for rustfs to start..."
          sleep 1;
        done
        AWS_ACCESS_KEY_ID=root AWS_SECRET_ACCESS_KEY=roottoor AWS_DEFAULT_REGION=us-east-1 \
          aws --endpoint-url http://127.0.0.1:9000 s3api create-bucket --bucket registry

        CARGO_INCREMENTAL=0 RUSTFLAGS='-Cinstrument-coverage' LLVM_PROFILE_FILE='cargo-test-%p-%m.profraw' cargo test --workspace
        grcov . --binary-path ./target/debug/deps/ -s . -t html --branch --ignore-not-existing --ignore "target/**" -o target/coverage/html
        grcov . --binary-path ./target/debug/deps/ -s . -t markdown --branch --ignore-not-existing --ignore "../*" --ignore "/*" --ignore "target/**"

        docker stop rustfs
    - name: Upload coverage report
      uses: actions/upload-artifact@v7
      with:
        name: coverage
        path: ./target/coverage/html/*
        if-no-files-found: error
        retention-days: 1

  build-conformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.26.5'

      - name: Build conformance tool
        run: |
          git clone --depth 1 https://github.com/opencontainers/distribution-spec.git distribution-spec
          cd distribution-spec/conformance && \
            go build -o oci-conformance . && \
            mv oci-conformance ../.. && \
            cd ../..

      - name: Download zot benchmark
        run: |
          curl -L -o zb https://github.com/project-zot/zot/releases/download/v2.1.15/zb-linux-amd64
          chmod +x zb

      - name: Build scrub gates
        run: |
          cargo build -p conformance-gates
          cp target/debug/scrub-gates .

      - name: Build the container
        run: |
          docker build -t ${CONFORMANCE_IMAGE_NAME} -f Dockerfile --build-arg RELEASE_MODE=release .
          docker save ${CONFORMANCE_IMAGE_NAME} -o conformance-image.tar

      - name: Upload test artifacts
        uses: actions/upload-artifact@v7
        with:
          name: conformance-tools
          path: |
            oci-conformance
            zb
            scrub-gates
            conformance-image.tar
          if-no-files-found: error
          retention-days: 1

  conformance:
    name: Conformance & E2E (${{ matrix.variant }})
    runs-on: ubuntu-latest
    needs: build-conformance
    strategy:
      fail-fast: false
      matrix:
        include:
          - variant: fs-memory-variable-parts
            description: filesystem backend with memory lock and variable parts
            config: config-fs-memory-variable-parts.toml
            s3: false
            redis: false
          - variant: s3-memory-variable-parts
            description: s3 backend with memory lock and variable parts
            config: config-s3-memory-variable-parts.toml
            s3: true
            redis: false
          - variant: s3-cas-variable-parts
            description: s3 CAS backend with variable parts
            config: config-s3-cas-variable-parts.toml
            s3: true
            redis: false
          - variant: s3-redis-variable-parts
            description: s3 backend with redis lock and variable parts
            config: config-s3-redis-variable-parts.toml
            s3: true
            redis: true
          - variant: s3-memory-uniform-parts
            description: s3 backend with memory lock and uniform parts
            config: config-s3-memory-uniform-parts.toml
            s3: true
            redis: false
          - variant: s3-cas-uniform-parts
            description: s3 CAS backend with uniform parts
            config: config-s3-cas-uniform-parts.toml
            s3: true
            redis: false
          - variant: s3-redis-uniform-parts
            description: s3 backend with redis lock, uniform parts
            config: config-s3-redis-uniform-parts.toml
            s3: true
            redis: true
          - variant: fs-redis-variable-parts
            description: filesystem backend with redis lock and variable parts
            config: config-fs-redis-variable-parts.toml
            s3: false
            redis: true
    steps:
      - uses: actions/checkout@v7

      - name: Download test artifacts
        uses: actions/download-artifact@v8
        with:
          name: conformance-tools

      - name: Load container image
        run: |
          docker load -i conformance-image.tar
          chmod +x oci-conformance zb scrub-gates

      - name: Start backend services
        run: |
          if [[ "${{ matrix.redis }}" == "true" ]]; then
            docker run --rm -d \
              --network host \
              --name redis-server \
              redis

            while ! nc -z localhost 6379; do
              echo "Waiting for redis to start..."
              sleep 1
            done
          fi

          if [[ "${{ matrix.s3 }}" == "true" ]]; then
            docker run --rm -d \
              -e RUSTFS_ACCESS_KEY=root \
              -e RUSTFS_SECRET_KEY=roottoor \
              --network host \
              --name rustfs \
              rustfs/rustfs:1.0.0-beta.8

            while ! curl -sf -o /dev/null http://127.0.0.1:9000/health; do
              echo "Waiting for rustfs to start..."
              sleep 1
            done

            # registry: primary; registry-b: replication instance B;
            # registry-split-{blob,meta}: the split-backend e2e scenario, which
            # needs a blob bucket no prior step has seeded with the test image.
            for bucket in registry registry-b registry-split-blob registry-split-meta; do
              AWS_ACCESS_KEY_ID=root AWS_SECRET_ACCESS_KEY=roottoor AWS_DEFAULT_REGION=us-east-1 \
                aws --endpoint-url http://127.0.0.1:9000 s3api create-bucket --bucket "$bucket"
            done
          fi

      - name: Generate e2e configs (${{ matrix.description }})
        run: |
          # Derive a backend-only fragment from the matrix config by dropping its
          # trailing access-policy + conformance-repository block, then append
          # each scenario's repository overlay. This runs the e2e workflows
          # against the exact backend under test.
          sed '/^\[global\.access_policy\]/,$d' "conformance/${{ matrix.config }}" > /tmp/backend.toml

          cat /tmp/backend.toml conformance/e2e/overlay-push.toml  > /tmp/e2e-push.toml
          cat /tmp/backend.toml conformance/e2e/overlay-cache.toml > /tmp/e2e-cache.toml
          cat /tmp/backend.toml conformance/e2e/overlay-replication-a.toml > /tmp/e2e-replication-a.toml

          # Instance B gets an isolated backend: a distinct port plus, when the
          # backend is shared across instances (S3 bucket, redis lock keyspace),
          # a distinct bucket and lock prefix. FS data is per-container already.
          sed -e 's/^port = 8000$/port = 8001/' \
              -e 's/bucket = "registry"/bucket = "registry-b"/' \
              -e 's/\(key_prefix = "[^"]*\)"/\1b"/' \
              /tmp/backend.toml > /tmp/backend-b.toml
          cat /tmp/backend-b.toml conformance/e2e/overlay-replication-b.toml > /tmp/e2e-replication-b.toml

          # Split-backend push scenario: put the blob and metadata stores on
          # distinct key spaces so a manifest stored in the wrong store (the
          # cross-store isolation bug) is unreadable on pull-back. BOTH stores
          # move to fresh spaces (S3 buckets / FS roots): the blob space must
          # NOT be one a prior step seeded, or the content-addressed manifest
          # blob would already be present and mask the bug, and its pre-existing
          # blobs would read as orphans under the fresh metadata store. The
          # conformance backend's fs root is host-mounted and shared, so the fs
          # blob root must move just like the S3 bucket does.
          # Only meaningful when the variant configures an explicit metadata
          # store; the "memory" variants inherit the blob store (single store).
          if grep -q '^\[metadata_store\.' /tmp/backend.toml; then
            if [[ "${{ matrix.s3 }}" == "true" ]]; then
              sed -e '/^\[blob_store\.s3\]$/,/^\[/ s/^bucket = .*/bucket = "registry-split-blob"/' \
                  -e '/^\[metadata_store\.s3\]$/,/^\[/ s/^bucket = .*/bucket = "registry-split-meta"/' \
                  /tmp/backend.toml > /tmp/backend-split.toml
            else
              sed -e '/^\[blob_store\.fs\]$/,/^\[/ s#^root_dir = .*#root_dir = "./conformance/blob-split-data"#' \
                  -e '/^\[metadata_store\.fs\]$/,/^\[/ s#^root_dir = .*#root_dir = "./conformance/metadata-split-data"#' \
                  /tmp/backend.toml > /tmp/backend-split.toml
            fi
            cat /tmp/backend-split.toml conformance/e2e/overlay-push.toml > /tmp/e2e-push-split.toml

            # Guard against a future config change silently defeating the rewrite
            # and turning this into a no-op regression test.
            blob_space=$(awk '/^\[blob_store\./{f=1} f&&/^(bucket|root_dir) =/{print; exit}' /tmp/backend-split.toml)
            meta_space=$(awk '/^\[metadata_store\./{f=1} f&&/^(bucket|root_dir) =/{print; exit}' /tmp/backend-split.toml)
            if [[ -z "$meta_space" || "$blob_space" == "$meta_space" ]]; then
              echo "split config did not diverge blob [$blob_space] from metadata [$meta_space]"; exit 1
            fi
            echo "SPLIT_BACKEND=true" >> "$GITHUB_ENV"
          else
            echo "SPLIT_BACKEND=false" >> "$GITHUB_ENV"
            echo "variant inherits the blob store (single store); skipping split-backend e2e"
          fi

          echo "===== backend fragment ====="
          cat /tmp/backend.toml

      - name: Start angos backend (${{ matrix.description }})
        run: |
          # On an fs store, the host-side scrub-gates binary reads what this
          # container writes, so both must run as the same uid or the reads
          # hit EACCES. s3 stores go over HTTP and need no --user.
          docker_user=""
          if [[ "${{ matrix.s3 }}" != "true" ]]; then docker_user="--user $(id -u):$(id -g)"; fi
          docker run --rm -d $docker_user \
            -v ./conformance/${{ matrix.config }}:/config.toml \
            -v "$PWD/conformance:/conformance" \
            --network host \
            --name conformance-${{ matrix.variant }}-backend \
            ${CONFORMANCE_IMAGE_NAME} \
            server

          if ! timeout 30 bash -c 'until nc -z -w 3 127.0.0.1 8000; do sleep 1; done'; then
            echo "angos did not start within 30 seconds"
            exit 1
          fi

      - name: Run conformance test (${{ matrix.description }})
        env:
          OCI_RESULTS_DIR: results
        run: |
          source ./conformance/conformance-variables
          ./oci-conformance

      - name: Upload conformance results (${{ matrix.description }})
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: conformance-results-${{ matrix.variant }}
          path: results/
          if-no-files-found: warn
          retention-days: 7

      - name: Run zot benchmark with scrub under live load (${{ matrix.description }})
        run: |
          zb_workdir="$(mktemp -d "${RUNNER_TEMP}/zb-conformance-${{ matrix.variant }}.XXXXXX")"
          ./zb --working-dir "$zb_workdir" -c 5 -r conformance -n 100 http://localhost:8000 &
          zb_pid=$!
          sleep 2

          # Scrub races the live benchmark traffic: the classify-then-recheck
          # lock discipline must never reclaim or repair in-flight state, so
          # even under load the walk must report all zeros. Run as the store's
          # owning uid on fs (see the backend step) so this run's tx-engine
          # writes stay readable by the later host-side gate binary.
          docker_user=""
          if [[ "${{ matrix.s3 }}" != "true" ]]; then docker_user="--user $(id -u):$(id -g)"; fi
          docker run --rm $docker_user \
            -e RUST_LOG=info \
            -v ./conformance/${{ matrix.config }}:/config.toml \
            -v "$PWD/conformance:/conformance" \
            --network host \
            ${CONFORMANCE_IMAGE_NAME} \
            scrub 2>&1 | tee scrub-under-load.log
          grep -E "scrub complete: walked [0-9]+ key\(s\): 0 repair\(s\), 0 quarantined, 0 corrupt deleted, 0 failure\(s\)" scrub-under-load.log
          walked=$(grep -oE "walked [0-9]+" scrub-under-load.log | grep -oE "[0-9]+")
          [ "$walked" -ge 50 ] || { echo "under-load walk saw only $walked keys; the listing is broken"; exit 1; }

          wait "$zb_pid"

      - name: Scrub gates (${{ matrix.description }})
        run: |
          export GATE_ANGOS_DOCKER_IMAGE=${CONFORMANCE_IMAGE_NAME}
          export GATE_CONFIG="$PWD/conformance/${{ matrix.config }}"
          if [[ "${{ matrix.s3 }}" == "true" ]]; then
            export GATE_MODE=s3 GATE_ENDPOINT=http://127.0.0.1:9000 GATE_BUCKET=registry
          else
            export GATE_MODE=fs
            GATE_DATA_ROOT=$(grep -m1 'root_dir' "conformance/${{ matrix.config }}" | cut -d'"' -f2 | sed 's|^\./||')
            export GATE_DATA_ROOT
            # The dockerized scrub/prune runs must write as the host binary's
            # uid so their output stays readable by this same-store gate.
            export GATE_DOCKER_USER="$(id -u):$(id -g)"
          fi
          export STATE_DIR="$RUNNER_TEMP/gate-state-${{ matrix.variant }}"
          mkdir -p "$STATE_DIR"

          # Push synthetic images so the store clears the gate floors
          # independently of how many manifests the conformance suite left.
          ./scrub-gates populate
          ./scrub-gates healthy
          ./scrub-gates corruption
          ./scrub-gates chaos
          ./scrub-gates manifest-content-type

          # Release port 8000 so the e2e scenarios can reuse the same backend.
          docker rm -f conformance-${{ matrix.variant }}-backend

      - name: Upload scrub gate state (${{ matrix.description }})
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: scrub-gate-state-${{ matrix.variant }}
          path: ${{ runner.temp }}/gate-state-${{ matrix.variant }}
          retention-days: 7

      - name: E2E - push and pull a Hub image
        run: |
          docker run -d --network host --name angos-push \
            -v "/tmp/e2e-push.toml:/config.toml" \
            ${CONFORMANCE_IMAGE_NAME} server

          for i in $(seq 1 30); do
            curl -sf -o /dev/null http://127.0.0.1:8000/v2/ && break
            [ "$i" -eq 30 ] && { echo "angos did not start"; docker logs angos-push; exit 1; }
            sleep 1
          done

          # Pull a real image from Docker Hub, retag it to the local instance and push.
          docker pull -q alpine:3.20
          docker tag alpine:3.20 localhost:8000/test/alpine:ci
          docker push localhost:8000/test/alpine:ci

          # Drop every local copy so the pull below re-downloads the blobs from angos.
          docker rmi -f localhost:8000/test/alpine:ci alpine:3.20
          docker pull -q localhost:8000/test/alpine:ci
          docker run --rm localhost:8000/test/alpine:ci /bin/true
          echo "push/pull round-trip OK"

          docker rm -f angos-push

      - name: E2E - push/pull with split blob+metadata stores
        if: env.SPLIT_BACKEND == 'true'
        run: |
          # Regression for cross-store isolation: the blob and metadata stores
          # live on different backends (distinct bucket / on-disk root). A
          # manifest must be stored as a blob in the blob store, or the manifest
          # GET and pull-back below 404.
          # On fs, this step plants aliens into and reads _lost_and_found from
          # the store on the host, so the container must own it as this uid.
          docker_user=""
          if [[ "${{ matrix.s3 }}" != "true" ]]; then docker_user="--user $(id -u):$(id -g)"; fi
          docker run -d $docker_user --network host --name angos-push-split \
            -v "/tmp/e2e-push-split.toml:/config.toml" \
            -v "$PWD/conformance:/conformance" \
            ${CONFORMANCE_IMAGE_NAME} server

          for i in $(seq 1 30); do
            curl -sf -o /dev/null http://127.0.0.1:8000/v2/ && break
            [ "$i" -eq 30 ] && { echo "angos did not start"; docker logs angos-push-split; exit 1; }
            sleep 1
          done

          docker pull -q alpine:3.20
          docker tag alpine:3.20 localhost:8000/test/alpine:split
          docker push localhost:8000/test/alpine:split

          # The manifest must be retrievable from the blob store (404 here was the bug).
          code=$(curl -s -o /dev/null -w '%{http_code}' \
            -H 'Accept: application/vnd.oci.image.index.v1+json,application/vnd.docker.distribution.manifest.list.v2+json,application/vnd.oci.image.manifest.v1+json,application/vnd.docker.distribution.manifest.v2+json' \
            http://127.0.0.1:8000/v2/test/alpine/manifests/split)
          [ "$code" = "200" ] || { echo "manifest GET returned $code (expected 200)"; docker logs angos-push-split; exit 1; }

          # Drop local copies so the pull below re-downloads the blobs from angos.
          docker rmi -f localhost:8000/test/alpine:split alpine:3.20
          docker pull -q localhost:8000/test/alpine:split
          docker run --rm localhost:8000/test/alpine:split /bin/true
          echo "split-backend push/pull round-trip OK"

          # A split deployment is where a cross-store misclassification would
          # quarantine live data; the walk must find nothing to change.
          docker run --rm $docker_user \
            -e RUST_LOG=info \
            -v "/tmp/e2e-push-split.toml:/config.toml" \
            -v "$PWD/conformance:/conformance" \
            --network host \
            ${CONFORMANCE_IMAGE_NAME} \
            scrub --dry-run 2>&1 | tee scrub-split.log
          grep -E "scrub complete: walked [0-9]+ key\(s\): 0 repair\(s\), 0 quarantined, 0 corrupt deleted, 0 failure\(s\)" scrub-split.log
          walked=$(grep -oE "walked [0-9]+" scrub-split.log | grep -oE "[0-9]+")
          [ "$walked" -ge 10 ] || { echo "split walk saw only $walked keys; the listing is broken"; exit 1; }

          # Plant an alien key in EACH store and require the quarantine to land
          # in the same store: the sharpest cross-store correctness proof.
          if [[ "${{ matrix.s3 }}" == "true" ]]; then
            s3() { AWS_ACCESS_KEY_ID=root AWS_SECRET_ACCESS_KEY=roottoor AWS_DEFAULT_REGION=us-east-1 \
              aws --endpoint-url http://127.0.0.1:9000 "$@"; }
            printf 'blob alien' | s3 s3 cp - s3://registry-split-blob/zz-split-alien/blob-side >/dev/null
            printf 'meta alien' | s3 s3 cp - s3://registry-split-meta/zz-split-alien/meta-side >/dev/null
          else
            blob_root=$(grep -m1 'root_dir' /tmp/e2e-push-split.toml | cut -d'"' -f2 | sed 's|^\./||')
            meta_root=$(grep -A2 'metadata_store' /tmp/e2e-push-split.toml | grep -m1 'root_dir' | cut -d'"' -f2 | sed 's|^\./||')
            mkdir -p "$blob_root/zz-split-alien" "$meta_root/zz-split-alien"
            printf 'blob alien' > "$blob_root/zz-split-alien/blob-side"
            printf 'meta alien' > "$meta_root/zz-split-alien/meta-side"
          fi

          docker run --rm $docker_user \
            -e RUST_LOG=info \
            -v "/tmp/e2e-push-split.toml:/config.toml" \
            -v "$PWD/conformance:/conformance" \
            --network host \
            ${CONFORMANCE_IMAGE_NAME} \
            scrub

          if [[ "${{ matrix.s3 }}" == "true" ]]; then
            [ "$(s3 s3 cp s3://registry-split-blob/_lost_and_found/zz-split-alien/blob-side - 2>/dev/null)" = "blob alien" ] \
              || { echo "blob-store alien not quarantined in the blob store"; exit 1; }
            [ "$(s3 s3 cp s3://registry-split-meta/_lost_and_found/zz-split-alien/meta-side - 2>/dev/null)" = "meta alien" ] \
              || { echo "metadata-store alien not quarantined in the metadata store"; exit 1; }
          else
            [ "$(cat "$blob_root/_lost_and_found/zz-split-alien/blob-side" 2>/dev/null)" = "blob alien" ] \
              || { echo "blob-store alien not quarantined in the blob store"; exit 1; }
            [ "$(cat "$meta_root/_lost_and_found/zz-split-alien/meta-side" 2>/dev/null)" = "meta alien" ] \
              || { echo "metadata-store alien not quarantined in the metadata store"; exit 1; }
          fi
          echo "cross-store quarantine landed in the correct stores"

          docker rm -f angos-push-split

      - name: E2E - pull through the cache from Docker Hub
        run: |
          docker run -d --network host --name angos-cache \
            -v "/tmp/e2e-cache.toml:/config.toml" \
            ${CONFORMANCE_IMAGE_NAME} server

          for i in $(seq 1 30); do
            curl -sf -o /dev/null http://127.0.0.1:8000/v2/ && break
            [ "$i" -eq 30 ] && { echo "angos did not start"; docker logs angos-cache; exit 1; }
            sleep 1
          done

          # First pull fetches the image from Docker Hub through angos and caches it.
          docker pull -q localhost:8000/docker-io/library/alpine:3.20
          # angos now serves the cached repository's tag list locally.
          curl -sf http://127.0.0.1:8000/v2/docker-io/library/alpine/tags/list | grep -q '"3.20"'
          # A second pull is served from the local cache.
          docker rmi -f localhost:8000/docker-io/library/alpine:3.20
          docker pull -q localhost:8000/docker-io/library/alpine:3.20
          docker run --rm localhost:8000/docker-io/library/alpine:3.20 /bin/true
          echo "pull-through cache OK"

          docker rm -f angos-cache

      - name: E2E - replicate a push from instance A to instance B
        run: |
          docker run -d --network host --name angos-repl-b \
            -v "/tmp/e2e-replication-b.toml:/config.toml" \
            ${CONFORMANCE_IMAGE_NAME} server
          docker run -d --network host --name angos-repl-a \
            -v "/tmp/e2e-replication-a.toml:/config.toml" \
            ${CONFORMANCE_IMAGE_NAME} server

          for i in $(seq 1 30); do
            curl -sf -o /dev/null http://127.0.0.1:8000/v2/ \
              && curl -sf -o /dev/null http://127.0.0.1:8001/v2/ && break
            [ "$i" -eq 30 ] && { echo "instances did not start"; docker logs angos-repl-a; docker logs angos-repl-b; exit 1; }
            sleep 1
          done

          # Push to A; the event path replicates the tag to B (in-process drain).
          docker pull -q alpine:3.20
          docker tag alpine:3.20 localhost:8000/repl/alpine:v1
          docker push localhost:8000/repl/alpine:v1

          # Wait for the tag to converge on B.
          for i in $(seq 1 60); do
            curl -s http://127.0.0.1:8001/v2/repl/alpine/tags/list | grep -q '"v1"' && break
            [ "$i" -eq 60 ] && { echo "replication did not converge"; docker logs angos-repl-a; exit 1; }
            sleep 1
          done

          # Drop local copies and pull the replicated image from B.
          docker rmi -f localhost:8001/repl/alpine:v1 localhost:8000/repl/alpine:v1 alpine:3.20
          docker pull -q localhost:8001/repl/alpine:v1
          docker run --rm localhost:8001/repl/alpine:v1 /bin/true
          echo "replication A -> B OK"

          docker rm -f angos-repl-a angos-repl-b

      - name: Dump logs
        if: always()
        run: |
          for c in conformance-${{ matrix.variant }}-backend angos-push angos-push-split angos-cache angos-repl-a angos-repl-b; do
            if docker inspect "$c" >/dev/null 2>&1; then
              echo "===== $c ====="
              docker logs "$c" || true
              docker rm -f "$c" >/dev/null 2>&1 || true
            fi
          done

          for svc in rustfs; do
            if docker inspect "$svc" >/dev/null 2>&1; then
              docker logs "$svc" || true
              docker stop "$svc" || true
            fi
          done

          if [[ "${{ matrix.redis }}" == "true" ]]; then
            docker logs redis-server || true
            docker stop redis-server || true
          fi

  build-binary:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            target: x86_64-apple-darwin
            binary_name: angos-darwin-amd64
          - os: macos-14
            target: aarch64-apple-darwin
            binary_name: angos-darwin-arm64
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            binary_name: angos-linux-amd64
            provider_binary_name: angos-credential-provider-linux-amd64
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-musl
            binary_name: angos-linux-arm64
            provider_binary_name: angos-credential-provider-linux-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7

      - name: Install musl tools (Linux)
        if: contains(matrix.target, 'linux')
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Install Rust toolchain
        run: |
          rustup target add ${{ matrix.target }}

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24.19.0'

      - name: Build release binary
        env:
          CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_RUSTFLAGS: "-Clink-self-contained=yes -Clinker=rust-lld"
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_RUSTFLAGS: "-Clink-self-contained=yes -Clinker=rust-lld"
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename binary
        run: mv target/${{ matrix.target }}/release/angos ${{ matrix.binary_name }}

      - name: Upload binary
        uses: actions/upload-artifact@v7
        with:
          name: binary-${{ matrix.binary_name }}
          path: ${{ matrix.binary_name }}
          if-no-files-found: error
          retention-days: 1

      # The kubelet credential provider is installed on nodes, it ships for
      # Linux alone.
      - name: Build kubelet credential provider
        if: contains(matrix.target, 'linux')
        env:
          CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_RUSTFLAGS: "-Clink-self-contained=yes -Clinker=rust-lld"
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_RUSTFLAGS: "-Clink-self-contained=yes -Clinker=rust-lld"
        run: |
          cargo build --release --target ${{ matrix.target }} -p kubelet-credential-provider
          mv target/${{ matrix.target }}/release/angos-credential-provider ${{ matrix.provider_binary_name }}

      - name: Upload kubelet credential provider
        if: contains(matrix.target, 'linux')
        uses: actions/upload-artifact@v7
        with:
          name: binary-${{ matrix.provider_binary_name }}
          path: ${{ matrix.provider_binary_name }}
          if-no-files-found: error
          retention-days: 1

  build-container:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        platform:
          - linux/amd64
          - linux/arm64
    steps:
      - name: Prepare
        run: |
          platform=${{ matrix.platform }}
          echo "PLATFORM_PAIR=${platform//\//-}" >> $GITHUB_ENV

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ${{ env.GHCR_REPO }}

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v7
        with:
          platforms: ${{ matrix.platform }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: RELEASE_MODE=release
          outputs: type=image,"name=${{ env.GHCR_REPO }}",push-by-digest=true,name-canonical=true,push=true

      - name: Export digest
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

      - name: Upload digest
        uses: actions/upload-artifact@v7
        with:
          name: digests-${{ env.PLATFORM_PAIR }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    runs-on: ubuntu-latest
    needs:
      - build-container
    steps:
      - name: Download digests
        uses: actions/download-artifact@v8
        with:
          path: /tmp/digests
          pattern: digests-*
          merge-multiple: true

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Install Cosign
        if: github.event_name != 'pull_request'
        uses: sigstore/cosign-installer@v4.1.2

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ${{ env.GHCR_REPO }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}

      - name: Create manifest list and push
        working-directory: /tmp/digests
        run: |
          docker buildx imagetools create $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
            $(printf '${{ env.GHCR_REPO }}@sha256:%s ' *)

      - name: Inspect image
        id: inspect
        run: |
          docker buildx imagetools inspect ${{ env.GHCR_REPO }}:${{ steps.meta.outputs.version }}
          digest=$(docker buildx imagetools inspect "${{ env.GHCR_REPO }}:${{ steps.meta.outputs.version }}" --format '{{json .Manifest}}' | jq -r '.digest')
          echo "digest=${digest}" >> "$GITHUB_OUTPUT"

      - name: Sign container image with Cosign
        if: github.event_name != 'pull_request'
        env:
          IMAGE: ${{ env.GHCR_REPO }}@${{ steps.inspect.outputs.digest }}
        run: cosign sign --yes "${IMAGE}"

  release:
    runs-on: ubuntu-latest
    needs:
      - merge
      - build-binary
      - rust-checks
      - rust-test
      - conformance
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v7

      - name: Download binaries
        uses: actions/download-artifact@v8
        with:
          path: /tmp/binaries
          pattern: binary-*
          merge-multiple: true

      - name: Install Cosign
        uses: sigstore/cosign-installer@v4.1.2

      - name: Sign binaries with Cosign
        working-directory: /tmp/binaries
        run: |
          for binary in angos-*; do
            cosign sign-blob --yes --bundle "${binary}.cosign.bundle" "${binary}"
          done

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: /tmp/binaries/*
          body: |
            ## Container Image

            ```
            docker pull ${{ env.GHCR_REPO }}:${{ github.ref_name }}
            ```

            ## Standalone Binaries

            | Platform             | Binary                 |
            |----------------------|------------------------|
            | macOS amd64 (Intel)  | `angos-darwin-amd64`   |
            | macOS arm64 (Apple)  | `angos-darwin-arm64`   |
            | Linux amd64          | `angos-linux-amd64`    |
            | Linux arm64          | `angos-linux-arm64`    |

            ## Kubelet Credential Provider

            Installed on nodes so image pulls authenticate with the pulling pod's
            service-account token. `contrib/kubelet-credential-provider` carries a
            DaemonSet that installs it from this release.

            | Platform    | Binary                                    |
            |-------------|-------------------------------------------|
            | Linux amd64 | `angos-credential-provider-linux-amd64`   |
            | Linux arm64 | `angos-credential-provider-linux-arm64`   |

            ## Verification with Cosign

            All release artifacts are signed with [Sigstore Cosign](https://docs.sigstore.dev/)
            using keyless signing backed by GitHub Actions OIDC. No key material is
            stored; verification relies on the public Sigstore transparency log.

            Install `cosign` from https://docs.sigstore.dev/cosign/installation/.

            ### Container image

            ```
            cosign verify ${{ env.GHCR_REPO }}:${{ github.ref_name }} \
              --certificate-identity-regexp="^https://github.com/${{ github.repository }}/\.github/workflows/build\.yaml@refs/tags/" \
              --certificate-oidc-issuer=https://token.actions.githubusercontent.com
            ```

            ### Standalone binaries

            Each binary ships with a companion `<binary>.cosign.bundle` file. Download
            both and verify, for example for the Linux amd64 binary:

            ```
            cosign verify-blob \
              --bundle angos-linux-amd64.cosign.bundle \
              --certificate-identity-regexp="^https://github.com/${{ github.repository }}/\.github/workflows/build\.yaml@refs/tags/" \
              --certificate-oidc-issuer=https://token.actions.githubusercontent.com \
              angos-linux-amd64
            ```
